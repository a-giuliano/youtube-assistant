# All of these are injected by `neon dev` and written by `neon env pull`
# (from the `neon.ts` policy + your linked branch). You normally don't edit them by hand.

NEON_BRANCH="main"

# Postgres (always present)
DATABASE_URL="postgresql://neondb_owner:...@ep-...-pooler.c-3.us-east-2.aws.neon.tech/neondb?channel_binding=require&sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://neondb_owner:...@ep-....c-3.us-east-2.aws.neon.tech/neondb?channel_binding=require&sslmode=require"

# Neon object storage (preview.buckets) — read automatically by the AWS S3 client
AWS_ACCESS_KEY_ID="nak_live_..."
AWS_SECRET_ACCESS_KEY="nsk_live_..."
AWS_ENDPOINT_URL_S3="https://<branch-id>.storage.c-3.us-east-2.aws.neon.tech"
AWS_REGION="us-east-2"

# Neon AI Gateway (preview.aiGateway) — OPENAI_* are read by @ai-sdk/openai;
# the @neondatabase/ai-sdk-provider reads NEON_AI_GATEWAY_*.
OPENAI_API_KEY="nt_live_..."
OPENAI_BASE_URL="https://<branch-id>-api.ai.c-3.us-east-2.aws.neon.tech/ai-gateway/openai/v1"
NEON_AI_GATEWAY_TOKEN="nt_live_..."
NEON_AI_GATEWAY_BASE_URL="https://<branch-id>-api.ai.c-3.us-east-2.aws.neon.tech"

# Neon Auth (auth: true) — written by `neon env pull`. The cookie secret is a
# per-app value you generate yourself with `openssl rand -base64 32` and is NOT
# managed by Neon.
NEON_AUTH_BASE_URL="https://<endpoint>.neonauth.c-3.us-east-2.aws.neon.tech/neondb/auth"
NEON_AUTH_JWKS_URL="https://<endpoint>.neonauth.c-3.us-east-2.aws.neon.tech/neondb/auth/.well-known/jwks.json"
NEON_AUTH_COOKIE_SECRET="generate-with-openssl-rand-base64-32"
